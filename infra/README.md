# RingIQ Infrastructure

Infrastructure and deployment configuration will live here when needed.

Examples:

- Docker compose for local dependencies.
- Database migration helpers.
- Deployment manifests.
- Observability configuration.

Keep production infrastructure decisions deferred until the implementation architecture is clearer.

