# RingIQ Packages

Shared internal packages live here.

Keep this folder small in v1. Add shared packages only when multiple apps genuinely need the same code.

Possible future packages:

- `python-common`: shared Python utilities, types, and integration contracts.
- `ui`: shared frontend components if the web app grows beyond a single app.

